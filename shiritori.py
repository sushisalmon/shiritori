def shiritori(x, y, i, list1, list2):
    if i == x :





f_name = input("Input file name:")

fp = open(f_name ,"r")

f_letter = fp.read()

all_list = f_letter.split("\n")

try:
    use_num = int(all_list[0])

except ValueError:
    print("１行目はしりとりで使う文字列の数にしてください！")


for pick in all_list:
    if pick >= "0" and pick "9":
        ans_list = list()
        str_num = a

        use_num = int(pick)
